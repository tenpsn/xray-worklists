#Use default
# ค่านี้ setup.bat (Windows) / setup.sh (Linux) จะเขียนทับให้เป็น path จริงของโปรเจกต์นี้อัตโนมัติ
# Windows เช่น D:\xray-worklists, Linux เช่น /root/xray-worklists
PROJECT_HOST_PATH=D:\xray-worklists
