# ใช้โดย docker-compose.yml เท่านั้น (ค่านี้จะถูกฝังเข้าไปใน frontend ตอน build)
# ต้องเป็น URL ที่เบราว์เซอร์ของผู้ใช้เรียกถึง backend ได้จริง เช่น http://<IP เครื่องนี้>:4000
NEXT_PUBLIC_API_URL=http://localhost:4000
