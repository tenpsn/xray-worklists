# ใช้เฉพาะตอนรัน frontend ตรงๆ ด้วย `npm run dev` นอก Docker
# ตอนรันผ่าน Docker ให้ใช้ค่า default http://backend:4000 จาก docker-compose.yml

# URL ภายในของ backend API ที่ frontend server ใช้ proxy request "/api/*" ไปหา (browser ไม่เห็นค่านี้)
BACKEND_INTERNAL_URL=http://localhost:4000
