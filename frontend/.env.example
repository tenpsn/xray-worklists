# ไฟล์นี้ไม่ได้ใช้ตอนรันผ่าน Docker (docker-compose ใช้ .env ที่ root แทน)
# เป็นแค่ตัวอย่างสำหรับ copy ไปทำ .env.local ตอนรัน frontend ตรงๆ ด้วย `npm run dev` (นอก Docker) เท่านั้น

# URL ของ backend API (โปรเจกต์แยกต่างหาก, คนละ process/port)
NEXT_PUBLIC_API_URL=http://localhost:4000
